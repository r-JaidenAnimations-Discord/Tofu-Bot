module.exports = (queue, track) => {
	// TODO: Implement?
	/**
	 * Ok so here's an idea
	 * when a track ends, we delete the now playing message
	 * so how do we get started here..
	 * I think i need to pass the message id trough something in the queue
	 */
}
